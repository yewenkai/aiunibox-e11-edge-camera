# 安全与隐私

这是一个具有 root 硬件控制能力的实验性局域网摄像头项目。

## 默认安全边界

- 控制页和 RTSP/WebRTC 端口当前没有应用层身份认证。
- HTTP API 包含云台、灯光和 IR-CUT 等会改变设备状态的操作。
- 项目只应运行在可信局域网，或通过由用户管理的可信 VPN 访问。
- 不要通过路由器端口转发、反向代理或公网 IP 直接暴露 8080、8554、8889。
- 不要使用项目监控没有明确授权的人员或场所。

## 已采取的措施

- API 只接受白名单中的 LED 节点名，避免把用户输入拼接为任意 root shell 路径。
- 电机步数、速度和灯光亮度都在应用内限制范围。
- 会导致样机电机持续励磁的硬件复位 API 已禁用。
- 仓库忽略证书私钥、APK、固件、厂商二进制和本地构建配置。

## 仍需完成

- 控制 API 身份认证与 CSRF 防护
- RTSP/WebRTC 访问认证
- TLS 终止或由可信 VPN 提供传输保护
- root 命令进一步收敛到最小权限的本地代理
- 依赖和发布产物的自动化安全扫描

发现安全问题时，请优先使用 GitHub 的 Private vulnerability reporting，不要在公开 Issue 中披露可直接利用的细节。
